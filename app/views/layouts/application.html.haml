!!!
%html
  %head
    %title Peculiarity
    %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}/
    = stylesheet_link_tag "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    .container
      %header.row
        .twelve.columns
          %h1=link_to 'Peculiarity', root_path
          - if flash[:notice]
            .alert-box=flash[:notice]
          - if flash[:alert]
            .alert-box.error=flash[:alert]
      = yield
      %p
      %footer.row
        .three.columns
          %span
            &copy;
            %a{:href => "http://github.com/mathias"}
              Matt Gauger
            2012.
        .three.columns
          %span
            Peculiarity is open source.
            %br/
            %a{:href => "https://github.com/mathias/peculiarity"}
              Clone it on Github.
        .one.columns
          =link_to("Home", root_path)
        .one.columns
          =link_to("About", about_path)
        .one.columns
          =link_to("Support", "https://github.com/mathias/peculiarity/issues")
        .two.columns
          -# %input{:type => "search"}
